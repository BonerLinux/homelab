apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pv.yaml
  - pvc.yaml
  - postgres.yaml
  - helmrelease.yaml

configMapGenerator:
  - name: immich-values
    files:
      - values.yaml

generatorOptions:
  disableNameSuffixHash: true
