apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
  namespace: immich

spec:
  interval: 30m

  chart:
    spec:
      chart: immich
      version: "0.12.0"
      sourceRef:
        kind: HelmRepository
        name: immich
        namespace: flux-system

  values:
    image:
      tag: v2.7.5

    redis:
      enabled: true

    server:
      service:
        type: NodePort
        nodePort: 32283

    immich:
      persistence:
        library:
          existingClaim: immich-library
